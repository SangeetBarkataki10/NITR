## Task

Finish `nitr::case001::add` so the same API supports the remaining required numeric types.

### Requirements
- Keep existing `int`, `float`, and `double` behavior working.
- Support `long long` addition so large integer inputs such as `add(100000000, 100000000)` work correctly.

### Constraints
- Do not modify `app/main.cc`.
- You may modify files under `src/` and add new files under `src/` if needed.
- Do not modify evaluator files.
