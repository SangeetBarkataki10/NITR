## Task

Implement `nitr::case001::add` so the existing program works for the integer path.
### Requirements
- Support `int` addition so `add(3, 4)` produces `7`.

### Constraints
- Do not modify `app/main.cc`.
- You may modify files under `src/` and add new files under `src/` if needed.
- Do not modify evaluator files.
